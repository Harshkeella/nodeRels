# Backend. One install, one uvicorn command:
#   pip install -r requirements.txt
#   uvicorn backend.main:app --reload
python-pptx>=1.0    # renders the .pptx
mcp>=1.29,<2
pyjwt>=2.9
python-dotenv>=1.0
pymupdf>=1.24
# Install the shared contract: pip install ../../packages/artifact-core
fastapi>=0.110      # the API
uvicorn>=0.27       # the server
pillow>=10.0        # image_engine: decode, validate, quality-score, perceptual-hash
pydantic>=2.0       # image_engine: schemas
httpx>=0.27         # fastapi TestClient, used by --selftest only
edge-tts>=7.0       # narrated explanation videos
imageio-ffmpeg>=0.6 # bundled FFmpeg binary for video assembly
pywin32>=306; platform_system == "Windows"  # render slides through PowerPoint
# Groq and the stock providers are plain stdlib urllib -- no SDK, no http client.
