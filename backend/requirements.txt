fastapi>=0.115
mcp>=1.29,<2
# Install the shared content/render package first: pip install ../packages/artifact-core
uvicorn[standard]>=0.32
python-multipart>=0.0.12
python-dotenv>=1.0
pydantic>=2.9

lightrag-hku>=1.4

sentence-transformers>=3.2
gliner>=0.2.20
pymupdf>=1.24
trafilatura>=1.12
httpx>=0.27
pyyaml>=6.0
youtube-transcript-api>=1.2
openpyxl>=3.1
duckdb>=1.1

aiosqlite>=0.20

# Verifies the auth provider's JWTs locally -- the API never calls the auth
# service on the request path, so a sign-in outage can't take retrieval down.
pyjwt>=2.9

opik>=1.7

# Vector store (embedded by default; QDRANT_URL points at a server instead)
qdrant-client>=1.12
